import { LoginForm } from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Login - ScopeFlow',
  description: 'Faça login na sua conta ScopeFlow',
};

export default function LoginPage() {
  return <LoginForm />;
}
