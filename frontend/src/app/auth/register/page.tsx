import { RegisterForm } from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Criar Conta - ScopeFlow',
  description: 'Crie uma nova conta ScopeFlow',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
